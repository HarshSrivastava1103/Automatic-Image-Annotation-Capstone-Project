# Automatic-Image-Annotation-Capstone-Project
This is a Streamlit web app that takes any uploaded image and automatically labels it with semantic tags using a deep learning model. Think of it as a simplified version of Google Photos' auto-tagging feature.
